machine:
  node:
    version: 6.0.0
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn
  post:
    - yarn global add bower
    - bower install
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test
